<h1> Welcome, <%= @merchant_user.name %>!</h1>
<br>
<center>
  <h3><%= @merchant.name %></h3>
  <h3><%= @merchant.address %></h3>
  <h3><%= "#{@merchant.city}, #{@merchant.state} #{@merchant.zip}" %></h3>
</center>
<br><br>

<h1 align = "center">Pending Orders</h1>
<center>
  <table>
    <tr>
      <th>Order ID</th>
      <th>Order Date</th>
      <th>Quantity of Items</th>
      <th>Order Total</th>
    </tr>
  <% @pending_orders.each do |item_order|%>
    <tr>
    <section id = "order-<%=item_order.id%>">
        <td><p>Order <%=link_to item_order.id, merchant_orders_show_path(item_order) %></p></td>
        <td><p><%= item_order.created_at.strftime('%D')%></p></td>
        <td><p><%= item_order.quantity%></p></td>
        <td><p><%= number_to_currency(item_order.grandtotal)%></p></td>
      </section>
    </tr>
  <% end %>
</table>
